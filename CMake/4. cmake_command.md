# cmake -G "xxx" #指定编译器，linux 系统下默认为gcc 编译器

# cmake -B build  # 创建build 文件夹，构建生成的中间文件和目标文件的生成路径

# cmake -S ./build #指定源文件根目录，必须包含一个 CMakeList.txt文件。 好像不需要，生成的文件，cmake -B build 这个过程会生成。

# cmake --build build # 第一个build 为编译的含义，第二个build 为位置